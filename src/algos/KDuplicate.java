package algos;

import java.util.ArrayList;
import java.util.List;

import graph.Arc;
import graph.DirectedGraph;

public class KDuplicate {
	
	private static List<>
	
	public static <T> DirectedGraph<T> duplicate (DirectedGraph<T> g, int k){
		List<Arc<T>> arcList = (List<Arc<T>>) g.getEdgeList();
		
		List<Arc<T>> newArcList = new ArrayList<>();
		
		for (Arc<T> arc : arcList) {
			
		}
		
		return new DirectedGraph<>(newArcList); 
	}
}
